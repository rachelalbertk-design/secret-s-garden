# The Secret's Garden Workspace

The GitHub-ready prototype lives here:

```text
outputs/the-secrets-garden-prototype
```

Use that folder as the repository root if you want the cleanest GitHub Pages setup. It contains the app files, README, ignore rules, and static-site metadata.
